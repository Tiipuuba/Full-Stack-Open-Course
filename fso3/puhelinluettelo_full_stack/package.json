{
  "name": "puhelinluettelo_full_stack",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "author": "Robert Brusila",
  "type": "commonjs",
  "main": "eslint.config.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../puhelinluettelo_front && npm run build && cp -r dist ../puhelinluettelo_full_stack",
    "deploy:full": "npm run build:ui && git add . && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "dotenv": "^17.2.4",
    "express": "^5.2.1",
    "mongoose": "^9.2.0",
    "morgan": "^1.10.1"
  }
}
